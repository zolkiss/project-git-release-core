[project]
name = "project-git-release-core"
version = "0.0.1"
dependencies = [
    "logging",
    "requests",
    "jsonpath-ng",
    "typer",
    "dotenv"
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["test"]
pythonpath = ["src"]